{% extends 'base.html.twig' %}

{% block body %}
    <h1>Projects creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary"/><a href="{{ path('projects_index') }}" class="btn btn-default">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
