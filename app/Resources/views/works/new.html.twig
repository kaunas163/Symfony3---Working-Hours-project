{% extends 'base.html.twig' %}

{% block body %}
    <h1>Works creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary" /><a href="{{ path('works_index') }}" class="btn btn-default">Back</a>
    {{ form_end(form) }}    

{% endblock %}
